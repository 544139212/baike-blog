#如不写，默认是用spring.application.name查找
spring.cloud.config.name=blog-jdbc
spring.cloud.config.uri=http://localhost:8083
#默认是default，可不写
#spring.cloud.config.profile=default
#默认是master，可不写
#spring.cloud.config.label=master
